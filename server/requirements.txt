# Google AI — Live API requires google-genai >= 0.8.0 (stable) or 1.x
google-genai>=1.0.0
# ADK is kept for the legacy fallback route (?backend=adk)
google-adk>=1.0.0
google-cloud-aiplatform>=1.60.0

# Firebase / Firestore
google-cloud-firestore>=2.16.0
firebase-admin>=6.5.0

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0
python-multipart>=0.0.9

# Image processing
pillow>=10.4.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
